# Already present on Colab Pro by default (indicative versions, adjust if
# Colab changes its base image):
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.3
pyyaml>=6.0

# To install explicitly on Colab (pip install -r requirements.txt) and in
# the future Dockerfile:
mne>=1.6
torch>=2.3
torch-geometric>=2.5
nnAudio>=0.3.3
